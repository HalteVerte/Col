/* recettes.js — migré vers recettes.json (session 16)
   Ce fichier est conservé pour compatibilité SW mais ne contient plus de données.
   RECIPES  → recettes.json
   TYPE_CFG → supprimé (non utilisé)
   CALQUES_ORDRE → supprimé (Leaflet retiré)
*/
